import styled from 'styled-components/native'

const LoadingWrapper = styled.View`
	flex: 1;
`

const LoadingImage = styled.Image`
	resize-mode:cover;
	width:100%;
	height: 100%;
`

export {LoadingWrapper,LoadingImage}
